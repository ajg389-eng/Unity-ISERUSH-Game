using UnityEngine;

/// <summary>
/// Marks this object as blocked on the worker pathfinding grid (walls, counters, etc.).
/// Cells under its colliders are treated as non-walkable.
/// </summary>
public class GridObstacle : MonoBehaviour
{
}
